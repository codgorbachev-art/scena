*** Begin Patch
*** Update File: index.html
@@ :root {
-    --fg: #fafafa;
-    --fg-subtle: #595959;
-    --accent: #7c3aed;
-    --bg: #08080a;
+    /* Improved contrast and unified accent color */
+    --fg: #d4d4d8;
+    --fg-subtle: #9ca3af;
+    --accent: #10b981;
+    --bg: #070708;
@@
     @media (max-width: 1024px) {
       .bento-grid {
         display: flex;
         flex-direction: column;
         gap: 2rem;
       }
     }
+    /* Additional responsive breakpoints for tablets and phones */
+    @media (max-width: 768px) {
+      .bento-grid {
+        display: flex;
+        flex-direction: column;
+        gap: 1.25rem;
+      }
+    }
*** End Patch

*** Update File: components/OptionCards.tsx
@@ type Item = { key: string; title: string; desc: string };
-             <motion.button
+             <motion.button
+               tabIndex={0}
@@
-               className={`relative overflow-hidden flex flex-col items-start text-left p-4 rounded-2xl transition-all duration-300 border ${
-                 isActive
-                   ? "bg-purple-600/10 border-purple-500 shadow-[0_0_20px_rgba(124,58,237,0.1)]"
-                   : "bg-zinc-900/40 border-zinc-800/50 hover:border-zinc-700/80"
-               }`}
+               className={`relative overflow-hidden flex flex-col items-start text-left p-4 rounded-2xl transition-all duration-300 border ${
+                 isActive
+                   ? "bg-emerald-600/10 border-emerald-500 shadow-[0_0_20px_rgba(16,185,129,0.1)]"
+                   : "bg-zinc-900/40 border-zinc-800/50 hover:border-zinc-700/80"
+               }`}
+               aria-pressed={isActive}
@@
-               <span
-                 className={`text-[12px] font-black uppercase tracking-tight mb-1 transition-colors ${
-                   isActive ? "text-purple-400" : "text-zinc-300"
-                 }`}
-               >
+               <span
+                 className={`text-[12px] font-black uppercase tracking-tight mb-1 transition-colors ${
+                   isActive ? "text-emerald-400" : "text-zinc-300"
+                 }`}
+               >
*** End Patch

*** Update File: components/ThumbnailCarousel.tsx
@@ const ThumbnailCard: React.FC<ThumbnailCardProps> = ({ 
-                 <img src={visual} className="w-full h-full object-cover select-none brightness-90 group-hover:brightness-105 transition-all duration-700" alt="Thumbnail" />
+                 <img src={visual} className="w-full h-full object-cover select-none brightness-90 group-hover:brightness-105 transition-all duration-700"
+                      alt={`Превью: ${idea}`} />
*** End Patch

*** Update File: App.tsx
@@
-               <textarea 
-                 value={text} 
-                 onChange={(e) => setText(e.target.value)}
-                 placeholder="Describe your vision..."
-                 className="w-full flex-1 bg-transparent text-xl font-black placeholder:text-zinc-800 outline-none resize-none leading-relaxed text-zinc-100 mb-8"
-               />
+               {/* Accessible input: aria-label & instructions */}
+               <textarea
+                 value={text}
+                 onChange={(e) => setText(e.target.value)}
+                 placeholder="Опишите, о чём будет ролик..."
+                 aria-label="Описание вашего ролика"
+                 aria-describedby="input-desc"
+                 className="w-full flex-1 bg-transparent text-xl font-black placeholder:text-zinc-800 outline-none resize-none leading-relaxed text-zinc-100 mb-8"
+               />
+               {/* Hidden descriptive text for screen readers */}
+               <p id="input-desc" className="sr-only">Опишите объект исследования, целевую аудиторию и желаемый стиль ролика.</p>
*** End Patch

*** Update File: components/Uploader.tsx
@@
-const TYPE_COLORS = {
+const TYPE_COLORS = {
@@
 };
+
+// Maximum file size (5 MB) constant for clarity
+const MAX_FILE_SIZE = 5 * 1024 * 1024;
@@
-    if (file.size > 5 * 1024 * 1024) {
-      updateFile(id, { status: "error", errorMessage: "Превышен лимит 5МБ" });
-      return;
-    }
+    if (file.size > MAX_FILE_SIZE) {
+      updateFile(id, { status: "error", errorMessage: "Превышен лимит 5МБ" });
+      return;
+    }
@@
-                 <div className="absolute bottom-0 left-0 right-0 h-1 bg-zinc-800/20">
-                    {f.status === "uploading" && (
-                      <motion.div 
-                        className="h-full bg-emerald-500"
-                        initial={{ width: 0 }}
-                        animate={{ width: `${f.progress}%` }}
-                      />
-                    )}
-                 </div>
+                 <div className="absolute bottom-0 left-0 right-0 h-1 bg-zinc-800/20">
+                    {f.status === "uploading" && (
+                      <motion.div 
+                        className="h-full bg-emerald-500"
+                        initial={{ width: 0 }}
+                        animate={{ width: `${f.progress}%` }}
+                        role="progressbar"
+                        aria-valuemin={0}
+                        aria-valuemax={100}
+                        aria-valuenow={f.progress}
+                      />
+                    )}
+                 </div>
*** End Patch
